{
  "name": "@takecontrol-money/hardhat",
  "scripts": {
    "generate-abi:fund": "hardhat generate-abi --contract Fund.sol --abipath ../website/abi",
    "generate-abi:orcastrator": "hardhat generate-abi --contract Orcastrator.sol --abipath ../website/abi",
    "generate-abi": "npm run generate-abi:fund && npm run generate-abi:orcastrator"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "hardhat": "^2.22.19"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0",
    "dotenv": "^16.4.7",
    "zod": "^3.24.2"
  }
}
